<div class="row">
  <div class="col-lg-12">
    <div class="box">
      <div class="body">
        <%= form_for([:admin, @cost_type]) do |f| %>
          <% if @cost_type.errors.any? %>
            <div id="error_explanation">
              <h2><%= pluralize(@cost_type.errors.count, "error") %> prohibited this cost_type from being saved:</h2>

              <ul>
                <% @cost_type.errors.full_messages.each do |message| %>
                  <li><%= message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>

          <div class="form-group">
            <%= f.label :名称 %><br>
            <%= f.text_field :name %>
          </div>
          <div class="form-group">
            <%= f.submit %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
